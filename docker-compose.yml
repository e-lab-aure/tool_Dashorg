version: "3.9"
services:
  dashorg:
    build: .
    container_name: dashorg
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
